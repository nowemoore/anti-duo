node_modules
dist
*.local
.DS_Store

# secrets / credentials — never commit these
.env
.env.*
!.env.example
*.pem
*.p8
*.p12
*.key
service-account*.json

# Internal planning / design notes — kept local, not published
planning/

# Runtime user progress (created on first run)
data/progress.json

# Generated static-demo content (rebuilt by `npm run build:static` / CI)
public/content.json


